using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cameranew1 : MonoBehaviour {

	// Use this for initialization
	void Start () {
		transform.Rotate (0, 0.5f, 0);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
